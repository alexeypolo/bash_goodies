# source me

# launch GDB and automatically run the given command with args
alias gdbrun="gdb -ex=r --args"

# Git diff with a graphical tool of your choice. Requires to add these two lines in ~/.gitconfig :
# [diff]
#        tool = gvimdiff
alias gdiff='git difftool --no-prompt'

